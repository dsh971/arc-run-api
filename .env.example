NODE_ENV=development
PORT=8080

# Supabase — get from supabase.com > Project Settings > API
SUPABASE_URL=
SUPABASE_ANON_KEY=          # Publishable key — safe for client use
SUPABASE_SERVICE_ROLE_KEY=  # Secret key — server only, never expose to clients
SUPABASE_JWT_SECRET=        # Optional — no longer used for JWT verification
DATABASE_URL=
